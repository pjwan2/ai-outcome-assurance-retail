# Test, lint, and load-test tooling only - never installed in the runtime
# Docker image (backend/Dockerfile installs requirements.txt alone).
-r requirements.txt

pytest==9.0.2
pytest-asyncio==1.3.0
httpx==0.28.1
httpx-sse==0.4.3
ruff==0.16.5
mypy==2.3.1
locust==2.46.4
pip-audit==2.10.1
