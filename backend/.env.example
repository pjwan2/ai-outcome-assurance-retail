DATABASE_URL=sqlite:///./ai_outcome_assurance.db
OPENAI_API_KEY=

# Bearer tokens for the demo API (app/auth.py), format: token:reviewer_id:role,...
# If unset, a single insecure default dev token is used so the demo runs zero-config.
# API_TOKENS=change-me-token:alice:retail-operations
